#ifndef TYPEDEFS_HPP
#define TYPEDEFS_HPP

#include <map>
#include <string>

typedef std::map<std::string, std::string>::const_iterator StringMapIterator;
typedef std::map<std::string, std::string> StringMap;

#endif