server {
    listen 127.0.0.1:8405;

    root /tmp/webserv_test_upload;

    location /upload {
        limit_except POST;
        upload_store /tmp/webserv_test_upload/uploads;
    }

    location / {
        index index.html;
    }
}
