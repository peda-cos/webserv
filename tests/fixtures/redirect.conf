server {
    listen 127.0.0.1:8408;

    root /tmp/webserv_test_redirect;

    location /old-page {
        return 301 /new-page;
    }

    location /temp-page {
        return 302 /other-page;
    }

    location / {
        index index.html;
    }
}
