server {
    listen 127.0.0.1:8409;

    root /tmp/webserv_test_bodylimit;
    client_max_body_size 1024;

    location /upload {
        limit_except POST;
        upload_store /tmp/webserv_test_bodylimit/uploads;
    }

    location / {
        index index.html;
    }
}
