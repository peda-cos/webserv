server {
    listen 127.0.0.1:8410;

    root /tmp/webserv_test_cookies;

    location /session {
        cgi_pass .py /usr/bin/python3;
        root /tmp/webserv_test_cookies/cgi;
    }

    location / {
        index index.html;
    }
}
